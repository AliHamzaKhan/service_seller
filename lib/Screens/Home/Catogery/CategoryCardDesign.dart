

import 'package:flutter/material.dart';

class CategoryCardDesign extends StatelessWidget {
  const CategoryCardDesign({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
