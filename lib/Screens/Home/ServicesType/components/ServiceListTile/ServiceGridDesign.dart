

import 'package:flutter/material.dart';

class ServiceGridDesign extends StatelessWidget {
  const ServiceGridDesign({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
