

import 'package:flutter/material.dart';

class ViewAllServicesScreen extends StatelessWidget {
  const ViewAllServicesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
