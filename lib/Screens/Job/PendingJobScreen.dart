

import 'package:flutter/material.dart';

class PendingJobScreen extends StatelessWidget {
  const PendingJobScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
