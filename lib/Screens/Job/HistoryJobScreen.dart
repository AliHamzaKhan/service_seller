

import 'package:flutter/material.dart';

class HistoryJobScreen extends StatelessWidget {
  const HistoryJobScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
